pipeline {
  agent any
    stages {
		stage('build') {
            steps {
                script{
                    sh (''' 
			 echo "las ramas que contiene el repo BBVA son: " + git branch -a
			
			
			           
		       ''')
                }       
            }                                              
        }		 
     }   
 }
