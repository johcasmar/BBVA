pipeline {
  agent any
    stages {
		stage('build') {
            steps {
                script{
                    sh (''' 
			 echo "la rama es: " + git branch --merged
			 
			 echo "esta es el vol : " + git branch -v              
		       ''')
                }       
            }                                              
        }		 
     }   
 }
