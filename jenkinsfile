pipeline {
  agent any
    stages {
		stage('build') {
            steps {
                script{
                    sh (''' 
			 git --version
			 git branch alt-history
			           
		       ''')
                }       
            }                                              
        }		 
     }   
 }
